# Runtime-only requirements for the FastAPI service (api.py).
# This is the lean set used by the Docker image — does NOT include
# Flask, SQLAlchemy, matplotlib, beautifulsoup, etc. that the Day-0
# Flask app drags in. Pinned loosely to match the rest of the repo.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
redis>=5.0

# RAG / sentiment / complaints champion stack
transformers==4.35.2
torch==2.1.1
sentence-transformers==2.2.2
faiss-cpu==1.7.4
vaderSentiment==3.3.2
nltk==3.8.1

# Trained complaint classifier serialization
scikit-learn==1.3.2
lightgbm>=4.0
joblib>=1.3
numpy==1.26.2
scipy>=1.10
